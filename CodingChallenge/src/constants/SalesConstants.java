package constants;

import java.math.BigDecimal;

public class SalesConstants {
    public static final BigDecimal SALES_TAX = BigDecimal.valueOf(0.10);
    public static final BigDecimal IMPORTED_TAX = BigDecimal.valueOf(0.05);
}
